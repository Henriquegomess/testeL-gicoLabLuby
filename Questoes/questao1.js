function questao1(){
    const resultado = []
    var entrada = prompt('Item array');
    var conteudo = prompt('Item array');

    while (isNaN(entrada)) {
        entrada = prompt('Item array');
    }
    
    for (let i = 0; i < entrada; index++) {
        result[i] = conteudo;
    }
    alert(resultado);

} 